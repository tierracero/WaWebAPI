# Candidate Work

Unapproved candidates derived from verified code. Promote an item to [TASKS.md](TASKS.md) only after scope and priority are approved.

- Resolve configuration key/source-of-truth inconsistency (`DEC-CONFIG-001`).
- Remove or redact token, secret, payload, HMAC, URL, and response debug logging.
- Design a configurable transport base URL and deterministic test seam.
- Replace the placeholder XCTest target with focused transport/encoding/decoding tests.
- Add `API.instance` with a compatibility-safe strategy for `API.instace`.
- Verify and resolve the `WaWebInstances` migration/model status enum mismatch.
- Define token-cache invalidation for expiry, rotation, and revocation.
- Consolidate duplicated route composition and response handling across `post` overloads.
- Remove obsolete commented transport implementation after confirming no needed behavior remains.
- Correct verified copy/paste method and migration names through compatibility-safe changes.
